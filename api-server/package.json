{
  "name": "resume-api-server",
  "version": "1.0.0",
  "description": "Node/Express REST API for the resume app: serves resume data and scores JD matches. A JS-equivalent of the Java Spring Boot service.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": { "node": ">=20" }
}
